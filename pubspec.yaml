name: github_graphql_client
description: Dart client for Github GraphQL
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  gql:
  gql_exec:

dev_dependencies:
  build_runner:
  gql_build:
  pedantic:

flutter:
  uses-material-design: true
